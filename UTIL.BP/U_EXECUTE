SUBROUTINE U_EXECUTE(STMT, VERBOSE)
$INCLUDE UTIL.BP UTIL.H

   IF VERBOSE THEN
      CALL U_CRT.ACTION("EXECUTE", STMT)
   END

   HUSH ON
   EXECUTE STMT
   HUSH OFF

   CALL U_LOG.CMD(STMT,"EXECUTE")

   RETURN
END
