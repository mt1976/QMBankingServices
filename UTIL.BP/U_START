SUBROUTINE U_START(PROGRAM.NAME)

   COMO.NAME = PROGRAM.NAME
   CALL U_UNIQUE.ID(COMO.NAME)

   STMT = "COMO ON ":COMO.NAME
   CALL U_CRT.ACTION("COMO", COMO.NAME)
   CALL U_EXECUTE("COMO OFF", @FALSE)
   CALL U_EXECUTE(STMT, @FALSE)

   PROGRAM.NAME = COMO.NAME
   RETURN
END
