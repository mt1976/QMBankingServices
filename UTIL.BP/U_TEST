PROGRAM U_TEST
** INFORMATION ****************************************************************
* Routine Name   : U_TEST
*         Type   : PROGRAM
*         Params : <<PARAMETERS>>
*         Loc    : UTIL.BP
* -----------------------------------------------------------------------------
* Info Updated   : 20191220 at 15.41.22 in MWT-QM-DEV by sales
*                : on ldn-srv-ubnt01 (Linux)
* -----------------------------------------------------------------------------
*
   CALL U_START("M.TEST")
   CALL U_HEADER("M.TEST")

   CALL U_CRT.INFO("COMMAND", @COMMAND)
   CALL U_CRT.INFO("SENTENCE", @SENTENCE)
   CALL U_CRT.INFO("LOGNAME", @LOGNAME)
   CALL U_CRT.INFO("TTY", @TTY)
   CALL U_CRT.INFO("USERNO", @USERNO)
   CALL U_CRT.INFO("PATH", @PATH)
   CALL U_CRT.INFO("WHO", @WHO)
   CALL U_CRT.INFO("SYS(33)", SYSTEM(33))
   CALL U_CRT.INFO("SYS(1015)", SYSTEM(1015))
   CALL U_CRT.INFO("IP",@IP.ADDR)

FOR I = 1 TO 91
CALL U_CRT.INFO("SYS-":I,SYSTEM(I))
NEXT I
FOR I = 1000 TO 1080
CALL U_CRT.INFO("SYS-":I,SYSTEM(I))
NEXT I
   VERBOSE = @TRUE
   ID.RESULT = ""

   *SQL.STATEMENT =  'select CodeMajorCurrencyIsoCode,CodeMinorCurrencyIsoCode from "dbo"."CurrencyPair" where Active=1'
   *SQL.DATABASE  =  ""

SQL.FIELDS   = "*"
SQL.TABLE    = "dbo.paymentSystems"
SQL.DATABASE = ""
SQL.WHERE    = ""
RES.COLUMNS  = ""
RES.DATA     = ""
RES.COUNT    = ""



*   CALL U_MSSQL.SELECT(SQL.FIELDS, SQL.TABLE, SQL.DATABASE, SQL.WHERE, VERBOSE, ID.RESULT,RES.COLUMNS,RES.DATA,RES.COUNT)

*   CALL U_CRT.INFO("RESULT", ID.RESULT)
*   CALL U_CRT.RECORD(RES.COLUMNS)
*   CALL U_CRT.RECORD(RES.DATA)
*   CALL U_CRT.INFO("NO RECORDS",RES.COUNT)

  * VAL = RND(9999)
  * NF = VAL:@VM:SQUOTE(VAL):@VM:SQUOTE(VAL):@VM:SQUOTE(VAL)

  * SQL.TABLE = SQL.TABLE
  * SQL.FIELDS = RES.COLUMNS
  * SQL.VALUES = SQUOTES(RES.DATA):@AM:NF
  * SQL.DATABASE = ""

   * CALL U_MSSQL.INSERT(SQL.TABLE, SQL.FIELDS, SQL.VALUES, SQL.DATABASE, VERBOSE)



   CALL U_STOP("")
   STOP
END
