SUBROUTINE M.CRT(R.RECORD)
$INCLUDE UTIL.BP UTIL.H
   NO.ROWS = DCOUNT(R.RECORD, @AM)

   FOR I = 1 TO NO.ROWS
      ROW = "<":I:">"
      CALL M.CRT.ACTION(ROW, R.RECORD<I>)
   NEXT I

   RETURN
END
